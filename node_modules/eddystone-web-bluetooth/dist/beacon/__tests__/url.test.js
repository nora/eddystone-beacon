"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var url_1 = require("../url");
var URL1 = 'https://example.com';
var URL2 = 'https://www.npmjs.com/features';
it('should decode the same value it has encoded', function () {
    var result1 = url_1.decodeUrl(url_1.encodeUrl(URL1));
    expect(result1).toBe(URL1);
    var result2 = url_1.decodeUrl(url_1.encodeUrl(URL2));
    expect(result2).toBe(URL2);
});
it('the encoded value should be as short as possible (by using prefixed codes)', function () {
    var result1 = url_1.encodeUrl(URL1);
    expect(result1.byteLength).toBe(9);
    var result2 = url_1.encodeUrl(URL2);
    expect(result2.byteLength).toBe(15);
});
//# sourceMappingURL=url.test.js.map